# jaccpot

JAX Fast Multipole Method solver package.

`jaccpot` contains multipole/local expansion operators, near/far-field evaluation,
and the high-level `FastMultipoleMethod` API. Tree construction and traversal are
provided by the companion package `yggdrasil`.
